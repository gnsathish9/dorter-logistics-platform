# Loadly Admin Portal

Static admin operations portal for tracking bookings, customers, driver partners, and payments.

## Run

Start the backend first:

```bash
cd backend
npm install
npm run dev
```

Then open `admin-portal/index.html` in a browser. If the backend is not running, the portal shows demo fallback data.
